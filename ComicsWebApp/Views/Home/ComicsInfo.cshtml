@model ComicsWebApp.Models.ComicsViewModel
@using ComicsWebApp.Utilities
@using ComicsWebApp.Data.Models

@{
    ViewData["Title"] = "ComicsInfo";
    var color = "black";
    if (Model.Comics.AvailabilityStatus.Equals(ComicsAvailabilityStatus.InStock))
    {
        color = "green";
    }
    else if (Model.Comics.AvailabilityStatus.Equals(ComicsAvailabilityStatus.SoldOut))
    {
        color = "red";
    }
}

<head>
    <link rel="stylesheet" href="~/css/ComicsInfo.css" asp-append-version="true" />
</head>

<h1>Comics Details</h1>

<hr />

<div class="modal fade" id="addPagesModal" tabindex="-1"
    aria-labelledby="addPagesModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <form enctype="multipart/form-data" method="post" asp-controller="Home" asp-route-id="@Model.Comics.Id" asp-action="AddPages">
                <div class="modal-header">
                    <h5 class="modal-title" id="AddPagesModalLabel">Add Comics Pages</h5>
                    <button id="closeAddPagesModalButton" type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <div class="form-group">
                    <label asp-for="Comics.Pages" class="control-label">Add Pages</label>
                    <input asp-for="Comics.Pages" class="form-control" type="file" name="pagesFiles" multiple />
                </div>
                <div class="modal-footer">
                    <input id="cancelAddPagesModalButton" type="button" class="btn btn-default" data-bs-dismiss="modal" value="Cancel" />
                    <input id="submitAddPagesModalButton" type="Submit" class="btn btn-success" value="Add" />
                </div>
                </div>
            </form>
        </div>
    </div>
</div>

<div class="flex-container">
    <div id="pagesCarousel" class="carousel slide pages-carousel" data-bs-ride="carousel" data-bs-interval="false">
        <div class="carousel-indicators">
            <button type="button" data-bs-target="#pagesCarousel" data-bs-slide-to="0" class="active" aria-current="true" aria-label="Slide 1"></button>
            @if (Model.Comics.Pages.Count > 0)
            {
                @for (int i = 1; i < Model.Comics.Pages.Count + 1; i++)
                {
                    <button type="button" data-bs-target="#pagesCarousel" data-bs-slide-to="@i" aria-label="Slide @i"></button>
                }
            }
        </div>
        <div class="carousel-inner" role="listbox">
            <div class="carousel-item active">
                <img src="@Url.Action("RenderPhoto", "Home", new {id = Model.Comics.Id })" />
            </div>
            @if (Model.Comics.Pages.Count > 0)
            {
                @foreach (var comicsPage in Model.Comics.Pages)
                {
                    <div class="carousel-item">
                        <img  src="@Url.Action("RenderPage", "Home", new {id = comicsPage.Id })" />
                    </div>
                }
            }
        </div>
        <a class="carousel-control-prev" href="#pagesCarousel" role="button" data-bs-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">Previous</span>
        </a>
        <a class="carousel-control-next" href="#pagesCarousel" role="button" data-bs-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">Next</span>
        </a>
    </div>
    <div class="flex-info-box">
        <div class="flex-container-space-between">
            <div class="flex-container-column-direction">
                <div style="color: @color;">@Model.Comics.AvailabilityStatus.GetDisplayName()</div>
                <h3>@Model.Comics.Name</h3>
            </div>
            <button type="button" class="btn btn-success button-edit">Edit</button>
        </div>
        <div class="flex-container-space-between">
            <div>
                <h3>$@Model.Comics.Price</h3>
                <div>Written by @Model.Comics.Author</div>
                <div><span class="span-bold">Publisher:</span> @Model.Comics.Publisher</div>
                <div>
                    <span class="span-bold">Genres:</span>
                    @foreach (var genre in Model.Comics.Genres)
                    {
                        <a href="">@genre.GenreName</a>
                    }
                </div>
            </div>
            <button id="showAddPagesModalButton" type="button" class="btn btn-primary button-pages" data-bs-toggle="modal" data-bs-target="#addPagesModal">
                Add Pages
            </button>
        </div>
    </div>
</div>

<h3>Description</h3>
<div>@Model.Comics.Description</div><br />

<div><span class="span-bold">Number of pages:</span> @Model.Comics.PagesNumber</div>
<div><span class="span-bold">Publication Format:</span> @Model.Comics.PublicationFormat</div>
<div><span class="span-bold">Cover Type:</span> @Model.Comics.CoverType</div>
<div><span class="span-bold">Year of publication:</span> @Model.Comics.YearOfPublication</div>
<div><span class="span-bold">Language:</span> @Model.Comics.Language</div>
<br /><br />

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
}